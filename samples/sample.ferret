

class Form
{
public:
	Form()
	{
		
	}

private:
	goal Move(const a, b, 1, "Hello")
	{
		a = 1;
		print("In Hello" + a + b);
		cut;
	}

	function Print(s)
	{
		::printf(s);
	}
}

Form += goal Move(_,_,"Bad");

var f = Form();
f.Move(1,2,"P");

